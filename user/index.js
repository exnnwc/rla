
$(document.body).ready(function () {
    if ($(document.body).attr('id') === "user_profile") {
        
    } else if $(document.body).attr('id') 
        console.log($(document.body).attr('id').substr(11, $(document.body).attr('id').length-11));{

    }
});
